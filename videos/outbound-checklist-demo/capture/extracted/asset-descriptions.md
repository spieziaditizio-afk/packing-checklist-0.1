# Asset inventory

No-capture path — the source is a local single-file HTML app
(`outbound-checklist.html`), not a live URL, so no screenshots or downloaded
assets exist. `capture/assets/` is intentionally empty.

Every visual frame in `STORYBOARD.md` will use `asset_candidates: []` (recreate
in HTML/CSS from the tokens and copy in `tokens.json` / `visible-text.txt`,
per the app's actual design system) rather than referencing a captured file.

Two prior repo artifacts exist for reference only, not as source assets for
this project:
- `brag-output/brag.mp4` — a separate, shorter (19.5s) hype-style cut of the
  same app. Different deliverable; not reused here.
- `outbound-checklist.html` itself — read directly for copy, CSS tokens, and
  behavior; not screen-captured.
